<h2>Order No: <%= @order.id %></h2>

<h5> <%= link_to "Home Page", '/' %></h5>
<%= link_to 'Factions', '/factions' %>
<%= link_to 'Contact Page', '/contact' %>
<%= link_to 'Shopping Cart', '/pages/cart' %>
<%= link_to 'Orders', '/orders' %>

<div>Order Status: <%= @order.status %></div>
<% @order.figure_orders.each do |f| %>
    <% @figure = f.figure %>
    <div>
        <%= render partial: 'figures/show', object: @figure %>
    </div>
<% end %>
Price: $<%= @order.total_price %>
Shipped To: <%= current_user.deliver_info %> <%= current_user.province.name %>